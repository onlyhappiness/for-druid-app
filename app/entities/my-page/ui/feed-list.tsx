import { StyleSheet, View } from "react-native";

const FeedList = () => {
  return <View></View>;
};

const styles = StyleSheet.create({
  container: {
    marginTop: 30,
  },
});

export default FeedList;
